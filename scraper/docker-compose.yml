services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"    # AMQP protocol (your scraper/worker connect here)
      - "15672:15672"  # Management UI (browser)
